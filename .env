# Disable source map generation in production for smaller bundle size
GENERATE_SOURCEMAP=false

# Keep ESLint enabled for code quality
DISABLE_ESLINT_PLUGIN=false

# Suppress browser opening on start (optional)
# BROWSER=none